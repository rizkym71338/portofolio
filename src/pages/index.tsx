import { LayoutMain } from "../layouts";

const PageHome = () => {
  return (
    <LayoutMain>
      <div className="h-screen">Konten</div>
      <div className="h-screen">Konten</div>
    </LayoutMain>
  );
};

export default PageHome;
